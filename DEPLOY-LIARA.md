# راهنمای آپلود دموی سایت روی لیارا

این راهنما برای نشان دادن سایت به کارفرما قبل از خرید دامنه و هاست است.
هر قدم را به ترتیب انجام بده. اگر جایی گیر کردی، پیام خطا را برایم بفرست.

**زمان تقریبی:** ۲۰ دقیقه · **هزینه:** حدود ۱۸ هزار تومان در روز

---

## قدم ۰ — پیش‌نیازها

- [ ] حساب لیارا ساخته شده ✔ (انجام شده)
- [ ] پروفایل حساب را **تایید کن** — در پنل لیارا پیام «تایید حساب کاربری» را
      دیدی؛ تا تاییدش نکنی نمی‌توانی سرویس بسازی
- [ ] کیف پول را شارژ کن — **۱۵۰ هزار تومان** برای یک هفته کافی است

---

## قدم ۱ — نصب ابزار لیارا

ترمینال را در پوشه‌ی پروژه باز کن و بزن:

```
npm install -g @liara/cli
```

بعد وارد حسابت شو:

```
liara login
```

ایمیل و رمز حساب لیارا را می‌خواهد.

---

## قدم ۲ — ساخت برنامه در پنل لیارا

۱. در پنل لیارا روی **«ایجاد برنامه»** بزن
۲. این تنظیمات را بده:

| گزینه | مقدار |
|---|---|
| نام برنامه | `giftland` (اگر گرفته شده بود، مثلاً `giftland-demo`) |
| پلتفرم | **Next.js** |
| پلن | **زمین** (ارزان‌ترین — برای دمو کافی است) |

۳. بعد از ساخت، آدرس سایتت می‌شود:
   **`https://giftland.liara.run`**
   (اگر نام دیگری گذاشتی، همان نام)

---

## قدم ۳ — تنظیم متغیرها ⚠️ مهم‌ترین قدم

در پنل برنامه، برو به بخش **«تنظیمات» → «متغیرهای محیطی»** و این هفت مورد
را یکی‌یکی اضافه کن:

| نام متغیر | مقدار |
|---|---|
| `DATABASE_URL` | همان رشته‌ی اتصال Neon از فایل `.env` |
| `AUTH_SECRET` | یک رشته‌ی تصادفی جدید (قدم ۳٫۱) |
| `NEXT_PUBLIC_BASE_URL` | `https://giftland.liara.run` |
| `DEMO_MODE` | `true` |
| `ZARINPAL_MODE` | `mock` |
| `SMS_PROVIDER` | `console` |
| `SEED_ADMIN_PHONE` | `09120000000` |

### قدم ۳٫۱ — ساخت کلید تصادفی

در ترمینال بزن و خروجی را در `AUTH_SECRET` بگذار:

```
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

> ⚠️ `NEXT_PUBLIC_BASE_URL` باید **قبل از آپلود** تنظیم شده باشد، وگرنه
> بازگشت از درگاه پرداخت به آدرس اشتباه می‌رود.
>
> ⚠️ آدرس را **بدون اسلش آخر** بنویس: `https://giftland.liara.run` ✔ نه
> `https://giftland.liara.run/` ✘

---

## قدم ۴ — آپلود

در ترمینال، داخل پوشه‌ی پروژه:

```
liara deploy --app giftland --port 3000
```

(اگر نام برنامه‌ات فرق دارد، همان را بنویس)

چند دقیقه طول می‌کشد. اول فایل‌ها آپلود می‌شوند، بعد روی سرور آلمان بیلد
می‌شود، بعد اجرا می‌شود.

**وقتی تمام شد، آدرس سایت را نشانت می‌دهد.**

---

## قدم ۵ — بررسی

آدرس `https://giftland.liara.run` را باز کن و این‌ها را چک کن:

- [ ] بالای صفحه یک **نوار زرد** با متن «نسخه‌ی نمایشی» دیده می‌شود
- [ ] محصولات و قیمت‌ها نمایش داده می‌شوند
- [ ] وارد شدن با شماره موبایل کار می‌کند و **کد در کادر بنفش** نشان داده می‌شود
- [ ] با شماره `09120000000` وارد شو → دکمه‌ی **«مدیریت»** ظاهر می‌شود

> اولین باز شدن ممکن است چند ثانیه طول بکشد، چون دیتابیس Neon از خواب
> بیدار می‌شود. دفعه‌ی بعد سریع است.

---

## چیزی که به کارفرما بگو

> «این نسخه‌ی نمایشیه. پرداخت واقعی انجام نمی‌شه و کد ورود به‌جای پیامک روی
> صفحه نشون داده می‌شه. بعد از تایید شما، دامنه و درگاه واقعی وصل می‌شن.»

**چیزهایی که ارزش نشان دادن دارند:**

۱. فیلتر و جستجوی زنده در صفحه‌ی محصولات
۲. تغییر ریجن در صفحه‌ی محصول → قیمت فوراً عوض می‌شود
۳. یک خرید کامل تا صفحه‌ی نتیجه (دکمه‌ی «پرداخت موفق» در درگاه ساختگی)
۴. کد تخفیف `WELCOME20`
۵. پنل مدیریت — مخصوصاً دکمه‌ی **«کپی آدرس»** در جزئیات سفارش

---

## بعد از تمام شدن دمو

برای اینکه هزینه ادامه پیدا نکند، برنامه را در پنل لیارا **حذف کن**.
هر وقت خواستی دوباره با همان دستور قدم ۴ بالا می‌آید.

---

## اگر به مشکل خوردی

| مشکل | راه‌حل |
|---|---|
| بیلد شکست خورد و گفت «Cannot find module …» | متن کامل خطا را برایم بفرست — یک دقیقه‌ای درست می‌شود |
| سایت بالا آمد ولی صفحه‌ها خطا می‌دهند | احتمالاً `DATABASE_URL` اشتباه است — دوباره از `.env` کپی کن |
| بعد از پرداخت به آدرس اشتباه می‌رود | `NEXT_PUBLIC_BASE_URL` را چک کن، بعد **دوباره deploy کن** |
| کد تایید نمایش داده نمی‌شود | `DEMO_MODE` باید دقیقاً `true` باشد، بعد دوباره deploy کن |

> نکته: بعد از تغییر هر متغیر محیطی، باید **دوباره `liara deploy` بزنی** تا
> اعمال شود.

---

## ⛔ قبل از افتتاح واقعی فروشگاه

`DEMO_MODE` را **حتماً حذف کن یا `false` بگذار**.

تا وقتی روشن است، هر کسی می‌تواند با شماره‌ی هر کس دیگری وارد حساب او شود.

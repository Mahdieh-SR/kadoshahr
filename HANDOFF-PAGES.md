# سند شروع: افزودن صفحه‌های «تماس با ما» و «درباره ما»

این فایل برای شروع یک گفتگوی تازه نوشته شده. همه‌ی چیزی که برای ادامه‌ی کار
لازم است اینجاست — نیازی به خواندن تاریخچه‌ی گفتگوی قبلی نیست.

---

## ۱. وضعیت فعلی پروژه

| | |
|---|---|
| نام سایت | **کادوشهر** (قبلاً «گیفت‌لند» بود — همه‌جا عوض شده) |
| دامنه | `kadoshahr.ir` — DNS و ریدایرکت `www` هر دو سالم |
| میزبانی | رانفلر، سرویس `giftland`، دیتابیس Postgres داخلی |
| فریم‌ورک | Next.js 16.3.3 با Turbopack، App Router |
| تست‌ها | ۱۲۷ تست محلی + ۱۲ تست روی سایت زنده، همه سبز |

### ⚠️ تنها مشکل باز

**گواهی SSL صادر نشده.** سرور هنوز `TRAEFIK DEFAULT CERT` را ارائه می‌دهد،
پس `https://kadoshahr.ir` در مرورگر هشدار امنیتی می‌دهد. DNS، دسترسی HTTP و
مسیر `/.well-known/acme-challenge/` همه بررسی و سالم‌اند؛ مشکل سمت پیکربندی
رانفلر است. این ربطی به کار صفحه‌های جدید ندارد و موازی پیگیری می‌شود.

---

## ۲. چطور تغییرات به سایت زنده می‌رود

رانفلر از گیت نمی‌خواند. مسیر استقرار:

```bash
# ۱. بسته را بساز (فقط فایل‌هایی که .gitignore اجازه می‌دهد)
#    ۲۸۳ فایل، حدود ۵۸۰ کیلوبایت
# ۲. در پنل: سرویس giftland → «استقرار جدید» → تب Drag & Drop → زیپ را بکش
# ۳. رانفلر خودش npm install و npm run build را می‌زند
```

بسته‌ی آماده: `kadoshahr-deploy.zip` در ریشه‌ی پروژه (در گیت نیست).

⚠️ **`node_modules`، `.next` و `.env` نباید در بسته باشند** — سرور خودش
می‌سازدشان.

---

## ۳. الگوهای کد که باید رعایت شوند

### جای صفحه‌ها

صفحه‌های عمومی داخل `src/app/(shop)/` می‌روند تا هدر و فوتر را بگیرند:

```
src/app/(shop)/contact/page.tsx     ← تماس با ما
src/app/(shop)/about/page.tsx       ← درباره ما
```

### الگوی یک صفحه‌ی ساده

`src/app/(shop)/payment-error/page.tsx` را به‌عنوان نمونه ببین. نکات:

```tsx
import type { Metadata } from "next";

export const metadata: Metadata = { title: "تماس با ما" };

export default function ContactPage() {
  return <div className="container-page py-20">…</div>;
}
```

- `metadata` با `title` — قالب سراسری خودش « | کادوشهر» را اضافه می‌کند
- کلاس `container-page` برای عرض یکسان با بقیه‌ی صفحات
- رنگ‌ها از توکن‌های موجود: `bg-ink-950`، `text-muted`، `border-ink-800`،
  `text-accent-400`، `rounded-card`
- زبان فارسی، جهت RTL — کل سایت `dir="rtl"` است

### چیزهایی که باید همراه صفحه به‌روز شوند

۱. **فوتر** — `src/components/site/Footer.tsx`
   ستون‌ها الان «فروشگاه» و «حساب کاربری» هستند. یک ستون سوم لازم است:

   ```ts
   const companyColumn = {
     title: "کادوشهر",
     links: [
       { href: "/about", label: "درباره ما" },
       { href: "/contact", label: "تماس با ما" },
     ],
   };
   ```

۲. **نقشه‌ی سایت** — `src/app/sitemap.ts`
   مسیرهای ثابت آنجا فهرست شده‌اند؛ `/about` و `/contact` باید اضافه شوند.

۳. **robots** — `src/app/robots.ts`
   این دو صفحه **نباید** در `disallow` باشند (برخلاف `/admin`، `/cart` و…).

---

## ۴. اطلاعاتی که باید از کاربر پرسیده شود

بدون این‌ها صفحه‌ها قابل نوشتن نیستند. **همه را یکجا بپرس، نه یکی‌یکی.**

### برای «تماس با ما»

| مورد | توضیح |
|---|---|
| شماره تلفن | ثابت یا موبایل؛ چند شماره هم ممکن است |
| ساعات پاسخگویی | مثلاً «شنبه تا چهارشنبه، ۹ تا ۱۸» |
| ایمیل | آدرس پشتیبانی |
| نشانی پستی | آیا اصلاً دفتر فیزیکی هست؟ برای کسب‌وکار دیجیتال ممکن است نباشد |
| کد پستی | معمولاً کنار نشانی لازم است |
| شبکه‌های اجتماعی | تلگرام، اینستاگرام، واتساپ — آیدی‌ها |
| فرم تماس | آیا فرم می‌خواهد یا فقط نمایش اطلاعات کافی است؟ ← **ببین بخش ۵** |

### برای «درباره ما»

| مورد | توضیح |
|---|---|
| سال شروع فعالیت | برای اعتمادسازی |
| نام ثبتی / شرکت | اگر شرکت ثبت‌شده دارد |
| چه چیزی متمایزش می‌کند | تحویل سریع؟ قیمت؟ پشتیبانی؟ |
| تعداد مشتری یا سفارش | فقط اگر عدد واقعی دارد — عدد ساختگی ننویس |
| متن دلخواه | آیا خودش متنی نوشته یا باید پیشنهاد بدهیم؟ |

⚠️ **هیچ عدد یا ادعایی از خودت نساز.** صفحه‌ی اصلی الان اعداد نمایشی دارد
(«۱۸٬۴۰۰+ سفارش موفق»، «۹٬۲۰۰+ مشتری») که از زمان نسخه‌ی دمو مانده‌اند.
اگر واقعی نیستند، بهتر است در همین فرصت با کاربر مطرح شوند.

---

## ۵. تصمیم فنی: فرم تماس

اگر کاربر **فرم** بخواهد (نه فقط نمایش اطلاعات)، این‌ها لازم می‌شود و باید
از قبل درباره‌شان تصمیم گرفت:

- **جدول جدید در دیتابیس** برای ذخیره‌ی پیام‌ها (`ContactMessage`)، چون
  ایمیل‌فرستادن روی این سرور تنظیم نشده است
- **صفحه در پنل مدیریت** برای دیدن پیام‌ها، وگرنه پیام‌ها جایی می‌مانند که
  کسی نمی‌بیندشان
- **محافظت ضدبات** — سایت الگویش را دارد: فیلد تله (`website`) در
  `src/lib/validation.ts` و `rateLimit` در `src/lib/rate-limit.ts`
- **مهاجرت دیتابیس** روی سرور: `npm run db:push`

اگر فقط نمایش اطلاعات باشد، هیچ‌کدام لازم نیست و کار در حد یک ساعت است.

**پیشنهاد:** برای شروع فقط نمایش اطلاعات + لینک مستقیم به تلگرام/واتساپ.
ساده‌تر، سریع‌تر، و برای مشتری ایرانی معمولاً کاربردی‌تر از فرم است.

---

## ۶. دستورهای مفید

```bash
npm run dev                  # سرور توسعه روی :3000
npm test                     # ۱۲۷ تست محلی (سرور dev باید بالا باشد)
npx tsc --noEmit             # بررسی نوع‌ها
LIVE_URL=https://kadoshahr.ir npm run test:live    # تست سایت زنده
```

⚠️ اگر سرور توسعه ساعت‌ها زیر فشار تست بماند، استخر اتصال دیتابیس ته می‌کشد
و خطای `P2028` می‌دهد. با ری‌استارت سرور حل می‌شود — باگ نیست.

---

## ۷. کارهای باز پروژه (ربطی به این صفحه‌ها ندارند)

- [ ] گواهی SSL روی رانفلر
- [ ] کد تخفیف — دیتابیس جدید هیچ کدی ندارد
- [ ] نرخ دلار: `۲۰۰٬۶۰۰` تومان، مربوط به ۲۸ آگوست ۲۰۲۶ — به‌روزرسانی لازم است
- [ ] `ZARINPAL_MODE` هنوز `mock` است — پرداخت واقعی انجام نمی‌شود
- [ ] `SMS_PROVIDER` هنوز `console` است — کد تایید فقط در لاگ سرور چاپ می‌شود
- [ ] لوگوی نینتندو (`public/brands/nintendo.svg`) — نبودنش خطا نمی‌دهد،
      خودکار به متن برمی‌گردد
- [ ] فید محصولات برای ترب — نیازمند نماد اعتماد الکترونیک

# راهنمای بالا آوردن سایت روی رانفلر (Runflare)

جایگزین `DEPLOY-LIARA.md` که حذف شد. هر قدم را به ترتیب انجام بده. اگر جایی
گیر کردی، پیام خطا را بفرست.

**زمان تقریبی:** ۴۵ دقیقه (بار اول)

---

## قدم ۰ — چه منابعی بخر

منابع در رانفلر روی **پروژه** خریده می‌شود و بین سرویس‌ها **تقسیم** می‌شود.
ما دو سرویس داریم: خود سایت، و دیتابیس Postgres.

اعداد زیر اندازه‌گیری‌شده‌ی همین پروژه است، نه حدس:

| اسلایدر | حداقل ممکن | **بخر** | چرا |
|---|---|---|---|
| RAM | ۵۱۲ MB | **۲ GB** | بیلد روی سرور پیک **۹۵۳ MB** می‌خورد. اجرا فقط ۱۷۷ MB است، ولی اگر بیلد OOM شود سایت اصلاً بالا نمی‌آید |
| CPU | ۰٫۲۵ GHz | **۲ GHz** | بیلد روی یک CPU دسکتاپ ۵۸ ثانیه طول می‌کشد. با ۰٫۲۵ GHz ده‌ها دقیقه می‌شود یا تایم‌اوت می‌خورد |
| Storage | ۱ GB | **۶ GB** | `node_modules` تنها ۶۹۱ MB است. ۱ GB قطعاً کم است |
| Deployment | ۱ | **۲** | یکی سایت، یکی Postgres |

> 💡 RAM و CPU بالا فقط **موقع بیلد** لازم است. اگر رانفلر اجازه‌ی کم و زیاد
> کردن منابع بعد از ساخت را بدهد، بعد از اولین بیلد موفق می‌شود پایین آورد.
> این را از پشتیبانی بپرس — ممکن است ماهی چند ده هزار تومان فرق کند.

---

## قدم ۱ — ساخت پروژه

۱. وارد [portal.runflare.com](https://portal.runflare.com) شو (ثبت‌نام + تایید
   ایمیل و شماره)
۲. **ایجاد پروژه** → اسلایدرها را طبق جدول بالا تنظیم کن

---

## قدم ۲ — ساخت دیتابیس Postgres

در پروژه، تب **«دیتابیس‌ها»** → **Postgres**.

منابعی که به دیتابیس می‌دهی از سهم پروژه کم می‌شود. برای این فروشگاه
(۳۳ محصول، ۱۲۴ نسخه) این کافی است:

| منبع | مقدار |
|---|---|
| RAM | ۵۱۲ MB |
| CPU | ۰٫۵ GHz |
| Storage | ۲ GB |

بعد از ساخت، **رشته‌ی اتصال** را بردار. دو حالت دارد:

- **آدرس داخلی** — برای وصل شدن خود سایت. سریع‌تر است، از اینترنت دیده
  نمی‌شود. این را در `DATABASE_URL` بگذار.
- **دسترسی از راه دور** — یک IP و پورت جدا. این را **موقتاً** روشن کن تا از
  کامپیوتر خودت داده‌ها را منتقل کنی (قدم ۵)، و بعد **خاموشش کن**.

---

## قدم ۳ — ساخت سرویس سایت

تب **«سرویس‌ها»** → **NodeJS** (یا NextJS اگر در لیست بود) → آخرین نسخه‌ی
Node که ارائه می‌دهد.

نام پیشنهادی: `giftland`

⚠️ **این سه مورد را حتماً در پنل چک کن** — مستندات رانفلر شفاف ننوشته و ممکن
است جای تنظیمشان فرق کند:

| مورد | مقدار درست |
|---|---|
| دستور بیلد | `npm install && npm run build` |
| دستور اجرا | `npm start` |
| پورت | `3000` |

`npm start` در این پروژه یعنی `node .next/standalone/server.js`. آن سرور
خودش `PORT` و `HOSTNAME` را از متغیر محیطی می‌خواند و پیش‌فرضش `3000` و
`0.0.0.0` است — یعنی روی کانتینر بدون تنظیم اضافه کار می‌کند.

اگر پنل «دستور بیلد» جدا ندارد و فقط یک «دستور اجرا» دارد، این را بگذار:

```
npm install && npm run build && npm start
```

---

## قدم ۴ — متغیرهای محیطی ⚠️ مهم‌ترین قدم

در تنظیمات سرویس سایت، این‌ها را اضافه کن:

| نام متغیر | مقدار |
|---|---|
| `DATABASE_URL` | آدرس **داخلی** Postgres از قدم ۲ |
| `AUTH_SECRET` | یک رشته‌ی تصادفی نو (پایین) |
| `NEXT_PUBLIC_BASE_URL` | آدرس سایت، **بدون اسلش آخر** |
| `ZARINPAL_MODE` | `mock` |
| `SMS_PROVIDER` | `console` |
| `SEED_ADMIN_PHONE` | `09120000000` |
| `UPLOAD_DIR` | `/app/uploads` — مسیر دیسک پایدار (قدم ۴٫۵) |

ساخت `AUTH_SECRET`:

```
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

> ⚠️ `AUTH_SECRET` رانفلر باید با لیارا **فرق کند**. کلید قدیمی روی سروری
> بوده که دیگر دستت نیست.
>
> ⚠️ `NEXT_PUBLIC_BASE_URL` باید **قبل از بیلد** ست باشد. این متغیر داخل
> فایل‌های ساخته‌شده می‌نشیند، پس تغییرش بعد از بیلد اثر ندارد — باید دوباره
> deploy کنی.
>
> ⚠️ اگر `DATABASE_URL` را دستی می‌نویسی و `sslmode` دارد، `verify-full`
> بگذار نه `require` (وگرنه درایور `pg` هشدار امنیتی چاپ می‌کند). برای
> دیتابیس داخلی رانفلر معمولاً اصلاً SSL لازم نیست.

### 🚨 `DEMO_MODE` را ست نکن

روی لیارا `DEMO_MODE=true` بود تا کد ورود روی صفحه نشان داده شود. **تا وقتی
روشن است، هر کسی می‌تواند با شماره‌ی هر کس دیگری وارد حساب او شود.** فقط اگر
باز هم نسخه‌ی نمایشی می‌خواهی ست کن، و قبل از افتتاح واقعی حذفش کن.

---

## قدم ۴٫۵ — دیسک پایدار برای تصاویر محصولات

بدون این قدم، دکمه‌ی «آپلود» در پنل مدیریت خطا می‌دهد.

سرویس → **«مدیریت دیسک»** → **«اضافه کردن دیسک»**:

| گزینه | مقدار |
|---|---|
| حجم | ۱ GB |
| سرویس | `giftland` |
| مسیر | `/app/uploads` |

⚠️ **چرا لازم است:** فایل‌سیستم کانتینر ناپایدار است. هر استقرار جدید یک
ایمیج تازه از روی کد می‌سازد، پس تصویری که بعد از بیلد روی دیسک نوشته شده
باشد از بین می‌رود — **بی‌سروصدا و بدون هیچ خطایی**. تصاویر چند ساعت بعد
۴۰۴ می‌دادند و علتش هم معلوم نمی‌شد.

فضا از مخزن پروژه کم می‌شود (نه از سرویس؛ سرویس فقط رم و CPU دارد). ۱ گیگ
برای هزاران تصویر محصول کافی است.

---

## قدم ۵ — ساخت جدول‌ها و انتقال داده

روی **کامپیوتر خودت**، با آدرس **دسترسی از راه دور** دیتابیس رانفلر:

```
# ۱. جدول‌ها را روی دیتابیس جدید بساز
DATABASE_URL="<آدرس راه‌دور رانفلر>" npm run db:push
```

بعد یکی از این دو را انتخاب کن:

### الف) شروع تمیز (پیشنهادی)

دیتابیس فعلی ۴۲۰ کاربر، ۱۳۹ سفارش و ۲۹۵ پرداخت ساختگی دارد که همه‌اش
زباله‌ی تست است. برای فروشگاه واقعی بهتر است از صفر شروع کنی:

```
DATABASE_URL="<آدرس راه‌دور رانفلر>" npm run catalog:apply
```

۲۷ محصول کاتالوگ ساخته می‌شوند، بدون هیچ سفارش و کاربر تستی.

### ب) انتقال کامل داده‌های فعلی

اگر می‌خواهی سفارش‌ها و کاربران فعلی هم بیایند:

```
SOURCE_DATABASE_URL="<آدرس Neon>" TARGET_DATABASE_URL="<آدرس رانفلر>" npm run db:copy -- --dry-run
SOURCE_DATABASE_URL="<آدرس Neon>" TARGET_DATABASE_URL="<آدرس رانفلر>" npm run db:copy
```

اول با `--dry-run` تعداد سطرها را ببین، بعد بدون آن اجرا کن. اسکریپت مقصد
غیرخالی را رد می‌کند و آخر کار تعداد سطرهای دو طرف را مقایسه می‌کند.

بعد از تمام شدن، **دسترسی از راه دور دیتابیس را خاموش کن.**

---

## قدم ۶ — آپلود

نصب CLI (یک بار):

```
# ویندوز
powershell -Command Invoke-WebRequest -Uri https://get.runflare.com/install.bat -OutFile install.bat; ./install.bat
```

بعد:

```
runflare login
runflare deploy
```

اولین بار باید داخل پوشه‌ی پروژه باشی؛ پروژه و سرویس را از لیست انتخاب
می‌کنی. دفعات بعد `runflare deploy -y` از انتخاب قبلی استفاده می‌کند.

> CLI رانفلر الگوهای `.gitignore` را رعایت می‌کند، یعنی `node_modules`،
> `.next`، `.env` و `src/generated` آپلود نمی‌شوند — درست همان چیزی که
> می‌خواهیم، چون سرور خودش `npm install` و `npm run build` را می‌زند و
> `prisma generate` داخل `build` هست.

در پنل، **ری‌استارت خودکار بعد از دیپلوی** را روشن کن تا تغییرات بلافاصله
اعمال شوند.

---

## قدم ۷ — بررسی

```
LIVE_URL=https://<آدرس سرویس> npm run test:live
```

۱۶ تست سرتاسری روی سایت زنده اجرا می‌شود: ورود، سبد، کد تخفیف، پرداخت،
هدرهای امنیتی و نقشه‌ی سایت.

دستی هم این‌ها را ببین:

- [ ] صفحه‌ی اصلی با **استایل و تصاویر** بالا می‌آید (اگر بی‌استایل بود،
      یعنی قدم `prepare-standalone` اجرا نشده — پایین را ببین)
- [ ] `/products` محصولات را نشان می‌دهد → یعنی دیتابیس وصل است
- [ ] ورود با شماره موبایل کار می‌کند
- [ ] با `09120000000` وارد شو → دکمه‌ی **«مدیریت»** ظاهر می‌شود

---

## اگر به مشکل خوردی

| مشکل | راه‌حل |
|---|---|
| بیلد وسط کار می‌ترکد / `heap out of memory` | RAM سرویس کم است. حداقل ۱٫۵ GB به خود سایت بده |
| سایت بالا می‌آید ولی **بدون استایل و تصویر** | `npm run build` کامل اجرا نشده. باید بعد از `next build` قدم `node scripts/prepare-standalone.mjs` هم بخورد — در `package.json` جزو `build` هست |
| `Cannot find module '.next/standalone/server.js'` | بیلد اصلاً اجرا نشده. دستور بیلد سرویس را چک کن |
| صفحه‌ها خطای دیتابیس می‌دهند | `DATABASE_URL` غلط است، یا جدول‌ها ساخته نشده‌اند (قدم ۵) |
| بعد از پرداخت به آدرس اشتباه می‌رود | `NEXT_PUBLIC_BASE_URL` را درست کن و **دوباره deploy کن** |
| سایت روی پورت جواب نمی‌دهد | پورت سرویس باید `3000` باشد، یا `PORT` را به‌عنوان متغیر محیطی ست کن |
| آپلود تصویر می‌گوید «مسیر آپلود قابل نوشتن نیست» | دیسک پایدار ساخته نشده یا `UPLOAD_DIR` با مسیر دیسک نمی‌خواند (قدم ۴٫۵) |
| تصاویر آپلودشده بعد از مدتی ۴۰۴ می‌دهند | یعنی روی دیسک ناپایدار نوشته شده‌اند — `UPLOAD_DIR` را با مسیر دیسک یکی کن |

---

## ⛔ قبل از افتتاح واقعی فروشگاه

- [ ] `DEMO_MODE` ست نباشد (یا `false`)
- [ ] `ZARINPAL_MODE` را از `mock` به حالت واقعی ببر و `ZARINPAL_MERCHANT_ID`
      را بگذار
- [ ] `SMS_PROVIDER` را از `console` به سرویس پیامک واقعی ببر — وگرنه کد
      تایید فقط در لاگ سرور چاپ می‌شود و به دست مشتری نمی‌رسد
- [ ] دسترسی از راه دور دیتابیس خاموش باشد

# وارد کردن انبوه محصولات — سند شروع جلسه‌ی جدید

> ابتدا `HANDOFF.md` را بخوان (وضعیت کلی پروژه و دام‌های محیط).
> این فایل فقط درباره‌ی کار پیشِ‌رو است: **پر کردن فروشگاه با محصولات واقعی.**

---

## هدف این مرحله

الان فروشگاه ۱۱ محصول **نمونه** دارد که هنگام راه‌اندازی ساخته شدند.
باید با محصولات واقعی جایگزین شوند — احتمالاً ده‌ها یا صدها محصول.

وارد کردن دستی از پنل ادمین برای این حجم منطقی نیست؛ **ابزار ورود گروهی
(CSV) لازم است.**

---

## ساختار داده — این را باید بفهمی

سه سطح تو در تو:

```
دسته‌بندی (Category)
   └── محصول (Product)
          └── نسخه (ProductVariant)   ← قیمت و موجودی اینجاست
```

**نکته‌ی کلیدی:** قیمت روی «محصول» نیست، روی «نسخه» است. هر ترکیب
پلتفرم/ریجن/حجم یک نسخه‌ی جداست با قیمت و موجودی مستقل.

مثال واقعی: «گیفت‌کارت پلی‌استیشن» یک محصول است با ۱۲ نسخه
(۳ ریجن × ۴ مبلغ).

### فیلدهای هر محصول

| فیلد | اجباری | توضیح |
|---|---|---|
| `title` | ✅ | عنوان فارسی |
| `slug` | ✅ | نشانی انگلیسی، فقط حروف کوچک و خط تیره |
| `description` | ✅ | حداقل ۱۰ کاراکتر |
| `categoryId` | ✅ | باید از دسته‌بندی‌های موجود باشد |
| `images` | — | آرایه‌ی آدرس تصویر (حداکثر ۸) |
| `specs` | — | لیست «عنوان/مقدار» برای تب مشخصات |
| `isActive` | ✅ | نمایش در فروشگاه |
| `isFeatured` | ✅ | نمایش در «پرفروش‌ترین‌ها» |

### فیلدهای هر نسخه

| فیلد | اجباری | توضیح |
|---|---|---|
| `platform` | ✅ | مثلاً `PlayStation Network` |
| `region` | ✅ | مثلاً `آمریکا` |
| `capacity` | ✅ | مثلاً `۵۰ دلاری` |
| `price` | ✅ | تومان، عدد صحیح، حداقل ۱۰۰۰ |
| `compareAtPrice` | — | قیمت قبل از تخفیف |
| `stock` | ✅ | موجودی |
| `isActive` | ✅ | |

`label` خودکار از سه فیلد اول ساخته می‌شود:
`"{platform} — {region} — {capacity}"`

قوانین اعتبارسنجی در `src/lib/validation.ts` → `adminProductSchema`.

---

## آنچه ساخته شد ✅

| | فایل |
|---|---|
| قالب CSV | `data/products-template.csv` |
| راهنمای فارسی برای صاحب پروژه | `data/products-guide.md` |
| اسکریپت ورود گروهی | `scripts/import-products.mjs` |
| دستورها | `npm run products:check` · `npm run products:import` |

نکته‌های پیاده‌سازی که ممکن است لازم شود بدانی:

- هر سطر = یک نسخه؛ سطرهای هم‌`slug` زیر یک محصول جمع می‌شوند.
- **برای محصولی که از قبل هست، خانه‌ی خالی یعنی «دست نزن».** یعنی می‌شود فایلی
  فقط با ستون‌های `slug,platform,region,capacity,price` داد و صرفاً قیمت‌ها را
  به‌روز کرد. (برای محصول جدید ستون‌های اجباری باید پر باشند.)
- نسخه‌ی موجود با کلید `platform+region+capacity` پیدا می‌شود — با نرمال‌سازی
  ارقام فارسی، نیم‌فاصله، ی/ك عربی و حروف بزرگ/کوچک. پس «۱۰ دلاری» و
  «10 دلاری» یک نسخه‌اند و نسخه‌ی تکراری ساخته نمی‌شود.
- **حذف نسخه‌ها پیش‌فرض نیست.** نسخه‌هایی که در فایل نیستند دست‌نخورده می‌مانند
  مگر با `--prune`. حتی با `--prune`، نسخه‌ای که در سفارشی استفاده شده حذف
  نمی‌شود و فقط `isActive=false` می‌گیرد (همان منطق `saveProduct`).
- اگر حتی یک خطا در فایل باشد، هیچ چیز نوشته نمی‌شود. خطاها با شماره‌ی سطرِ
  اکسل و به فارسی گزارش می‌شوند.
- خواننده‌ی CSV خودش BOM اکسل، پایان خط ویندوزی، جداکننده‌ی `,` / `;` / تب،
  گیومه و ستون‌های فارسی را می‌فهمد.
- نوشتن با `pg` خام و **یک تراکنش کوتاه به ازای هر محصول** انجام می‌شود —
  به‌خاطر دام شماره ۳ در `HANDOFF.md` (مهلت تراکنش روی Neon فرانکفورت).

باقی‌مانده از این بخش: **صفحه‌ی ورود گروهی در پنل ادمین** (بند ۳ پایین) که
اختیاری بود و ساخته نشد.

---

## طراحی اولیه (برای مرجع)

**۱. قالب CSV** که صاحب پروژه (که کد نمی‌نویسد) بتواند در اکسل پر کند.

پیشنهاد: هر سطر = یک **نسخه**، و ستون‌های محصول تکرار شوند. اسکریپت
سطرهای هم‌`slug` را زیر یک محصول جمع می‌کند.

```csv
slug,title,category,description,images,platform,region,capacity,price,stock
steam-wallet,گیفت‌کارت استیم,gaming,توضیحات…,/products/steam-1.svg,Steam,آمریکا,۱۰ دلاری,800000,25
steam-wallet,گیفت‌کارت استیم,gaming,توضیحات…,/products/steam-1.svg,Steam,ترکیه,۱۰ دلاری,690000,25
```

**۲. اسکریپت وارد کردن** — `scripts/import-products.mjs`

باید:
- CSV را بخواند و **قبل از نوشتن، کامل اعتبارسنجی کند**
- خطاها را با شماره‌ی سطر و به فارسی گزارش دهد
- حالت `--dry-run` داشته باشد (فقط بررسی، بدون تغییر دیتابیس)
- محصول موجود با همان `slug` را **به‌روزرسانی** کند، نه دوباره بسازد
- نسخه‌هایی که در سفارشی استفاده شده‌اند را حذف نکند، فقط غیرفعال کند
  (منطقش در `saveProduct` در `src/app/actions/admin.ts` هست — همان را دنبال کن)

**۳. صفحه‌ی ورود گروهی در پنل ادمین** (اختیاری، اگر وقت بود)
`/admin/products/import` — آپلود CSV با پیش‌نمایش قبل از تایید.

---

## تصاویر محصولات

الان تصاویر نمونه‌ی SVG در `public/products/` هستند که با
`prisma/generate-images.ts` ساخته شده‌اند.

برای محصولات واقعی سه راه هست:
1. تصاویر رسمی برند (بهترین کیفیت، از صفحات press/brand هر شرکت)
2. تصاویری که خود کارفرما تهیه می‌کند
3. همین SVGهای تولیدشده تا وقتی تصویر واقعی آماده شود

آدرس تصویر می‌تواند مسیر داخلی (`/products/x.png`) یا لینک کامل اینترنتی
باشد — کد هر دو را می‌پذیرد (با تگ `<img>` ساده نمایش داده می‌شود).

⚠️ **تصاویر و متن توضیحات را از سایت رقبا کپی نکن.** هم مشکل حقوقی دارد و
هم گوگل محتوای تکراری را در نتایج پایین می‌برد. توضیحات باید اورجینال باشد.

---

## دسته‌بندی‌های فعلی

```
gaming          گیفت‌کارت گیمینگ
subscriptions   اشتراک سرویس‌ها
shopping        گیفت‌کارت خرید
ai-tools        سرویس‌های هوش مصنوعی
```

اگر دسته‌ی جدیدی لازم شد، باید در دیتابیس اضافه شود (مدل `Category` با
فیلدهای `name`, `slug`, `icon`, `sortOrder`).
آیکون‌های مجاز: `gamepad`, `play`, `bag`, `spark`
(در `src/components/ui/icons.tsx` → `categoryIcons`).

---

## چیزهایی که باید از صاحب پروژه بپرسی

۱. **چند محصول و چند نسخه؟** (برای انتخاب روش)
۲. **داده‌ها الان کجاست؟** لیست قیمت تأمین‌کننده؟ اکسل؟ دستی؟
۳. **دسته‌بندی جدید لازم است؟**
۴. **تصاویر از کجا می‌آید؟**
۵. **محصولات نمونه‌ی فعلی حذف شوند یا بمانند؟**

---

## قبل از شروع

```
npm run dev        سرور باید بالا باشد
npm test           ۹۸ تست باید سبز باشد
```

بعد از هر تغییر در ساختار محصولات، دوباره `npm test` بزن — تست‌های
`test-admin-products.mjs` مستقیم همین منطق را بررسی می‌کنند.

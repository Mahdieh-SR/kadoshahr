# وضعیت پروژه — سند تحویل به جلسه‌ی بعد

> این فایل برای ادامه‌ی کار در یک گفتگوی جدید نوشته شده است.
> **قبل از هر کاری این فایل را کامل بخوان.**
> راهنمای کاربردی برای صاحب پروژه در `README.md` است.

آخرین به‌روزرسانی: پایان فاز ۷ (کد تخفیف و نظرات)

---

## ۱. پروژه چیست

فروشگاه گیفت‌کارت و اکانت دیجیتال، فارسی و راست‌به‌چپ.
صاحب پروژه یک **طراح فریلنسر است که کد نمی‌نویسد**.

### قواعد کار با او (رعایتش کن)

- همه‌ی توضیحات به **فارسی ساده**، بدون اصطلاح فنی
- بعد از هر تغییر بگو **«چه چیزی درست شد»** و **«چطور تستش کند»** — یعنی
  دقیقاً چه آدرسی را باز کند و روی چه چیزی کلیک کند
- قبل از مراحل بزرگ (مثل اتصال درگاه واقعی) از او تایید بگیر
- اگر کلید یا حسابی لازم است، دقیق بگو از کجا بگیرد و کجا بگذارد

---

## ۲. وضعیت فعلی: چه چیزی ساخته شده

همه‌ی موارد زیر **کامل، تست‌شده و کارکردی** هستند:

| بخش | وضعیت |
|---|---|
| صفحه اصلی، دسته‌بندی، فهرست محصولات | ✅ |
| فیلتر (پلتفرم/ریجن/بازه قیمت) + جستجوی زنده | ✅ |
| صفحه محصول: گالری، وردایانت وابسته، تب‌ها، مشابه‌ها | ✅ |
| سبد خرید | ✅ |
| ورود/ثبت‌نام با موبایل + کد OTP | ✅ |
| آدرس پستی (۳۱ استان + شهرها) | ✅ |
| تسویه‌حساب + درگاه + تایید سمت سرور + صفحه نتیجه | ✅ (درگاه **ساختگی**) |
| پنل کاربری (پیشخوان، سفارش‌ها، ویرایش حساب) | ✅ |
| پنل ادمین (سفارش، محصول، تخفیف، نظرات) | ✅ |
| کد تخفیف (درصدی/مبلغی، سقف، انقضا، محدودیت) | ✅ |
| قیمت‌گذاری دلاری + صفحه‌ی نرخ دلار در پنل ادمین | ✅ |
| ورود گروهی محصولات از CSV | ✅ |
| نظرات محصول + تایید توسط ادمین | ✅ |
| صفحات ۴۰۴/خطا/بارگذاری، SEO، sitemap، هدرهای امنیتی | ✅ |

**۱۱۳ تست خودکار — همه سبز.** با `npm test` اجرا می‌شوند.

---

## ۳. کارهای باقی‌مانده

### الف) اتصال زرین‌پال واقعی — منتظر کارفرما

کد کاملاً آماده است (`src/lib/zarinpal.ts`). فقط دو خط در `.env`:

```
ZARINPAL_MODE="live"
ZARINPAL_MERCHANT_ID="کد ۳۶ کاراکتری"
```

⚠️ درگاه واقعی روی `localhost` کار نمی‌کند — به دامنه‌ی ثبت‌شده نیاز دارد.
پس ترتیب منطقی: **اول انتشار روی دامنه، بعد اتصال درگاه.**
`ZARINPAL_MODE="sandbox"` هم برای تست بدون پول واقعی وجود دارد.

### ب) پنل پیامک — منتظر کارفرما

`src/lib/sms.ts` آماده است و فقط یک شاخه‌ی جدید می‌خواهد.
الان `SMS_PROVIDER="console"` است و کد تایید روی صفحه نمایش داده می‌شود.

### ج) انتقال دیتابیس — تصمیم گرفته شده، زمانش نرسیده

الان روی **Neon رایگان (فرانکفورت)** است. کاربر گفته بعداً عوض می‌کنیم.
دلیل اهمیت: پلن رایگان می‌خوابد، بکاپ تضمین‌شده ندارد، و فاصله‌ی جغرافیایی
باعث کندی می‌شود (یک باگ واقعی از همین ناشی شد — بخش ۵ را بخوان).

### د) انتشار روی سرور

چک‌لیست کامل در انتهای `README.md` است.

---

## ۴. نقشه‌ی فنی

**استک:** Next.js 16.3.3 (App Router, Turbopack) · TypeScript · Tailwind 4 ·
Prisma 7.10 · PostgreSQL (Neon) · NextAuth v5 beta · Zod 4

### فایل‌های کلیدی

```
src/lib/prisma.ts        اتصال دیتابیس + لایه‌ی تلاش مجدد خودکار
src/lib/pricing.ts       قیمت‌گذاری سبد — قلب امنیت مالی
src/lib/exchange-rate.ts نرخ دلار و بازنویسی قیمت نسخه‌های دلاری
src/lib/discount.ts      ارزیابی کد تخفیف
src/lib/zarinpal.ts      درگاه (mock / sandbox / live)
src/lib/rate-limit.ts    محدودیت نرخ، با پشتوانه‌ی دیتابیس
src/lib/session.ts       requireUser / requireAdmin / getAdminOrNull
src/lib/reviews.ts       بررسی خرید قبل از ثبت نظر
src/lib/validation.ts    همه‌ی اسکیماهای Zod
src/content/home.ts      متن‌های صفحه اصلی (کاربر خودش ویرایش می‌کند)
src/data/iran-locations.ts  ۳۱ استان و شهرها

src/app/api/payment/verify/route.ts   ⭐ مهم‌ترین فایل پروژه
src/app/actions/                       Server Actionها
```

### اصول امنیتی که در کل پروژه رعایت شده — **نشکنشان**

1. **هیچ عددی از مرورگر پذیرفته نمی‌شود.** قیمت، تخفیف و مبلغ کل همیشه
   سمت سرور از دیتابیس محاسبه می‌شوند. از ورودی کلاینت فقط `variantId` و
   `quantity` خوانده می‌شود.
2. **تایید پرداخت فقط سرور‌به‌سرور.** پارامتر `Status` در آدرس بازگشتی هرگز
   مبنای «موفق بودن» نیست — کاربر می‌تواند دستکاری‌اش کند.
3. **بررسی نقش ادمین داخل خود Server Action است**، نه فقط در layout صفحه.
   Server Actionها از بیرون هم قابل فراخوانی‌اند.
4. **محدودیت نرخ روی همه‌ی مسیرهای حساس** — OTP، ورود، تسویه، کد تخفیف،
   نظر، عملیات ادمین.
5. **شمارش استفاده از کد تخفیف بر مبنای سفارش‌های پرداخت‌شده** است، نه
   شمارنده‌ی `usedCount` (که فقط برای نمایش است).
6. **قیمت دلاری هم از کلاینت پذیرفته نمی‌شود.** فرم محصول یک عدد تومانی
   می‌فرستد ولی `saveProduct` برای نسخه‌های `usdPriced` آن را دور می‌ریزد و
   خودش از `priceUsd` × نرخ حساب می‌کند. تست `test-pricing` همین را می‌سنجد.

### کادرهای انتخاب صفحه‌ی محصول

هر محصول سه محور دارد (`platform` / `region` / `capacity`) ولی عنوانی که
مشتری می‌بیند فرق می‌کند: گیفت‌کارت «گیفت کارت» می‌خواهد، پلی‌استیشن پلاس
«پلن اکانت» و «ظرفیت اکانت». این عنوان‌ها در `Product.optionLabels` ذخیره
می‌شوند و از پنل ادمین هم قابل ویرایش‌اند.

- **شکل داده آرایه است، نه شیء** — چون JSONB ترتیب کلیدهای یک شیء را نگه
  نمی‌دارد ولی ترتیب آرایه را نگه می‌دارد، و ترتیب همان چیزی است که مشتری
  می‌بیند.
- محوری که در فهرست نباشد کادرش پنهان می‌شود، **مگر اینکه بیش از یک مقدار
  داشته باشد** — وگرنه مشتری راهی برای انتخابش نداشت.
- «ناموجود» با `stock = 0` نشان داده می‌شود، نه با `isActive = false` —
  وگرنه نسخه از کوئری فروشگاه فیلتر می‌شد و صفحه‌ی محصول بدون هیچ کادری
  رندر می‌شد.

### قیمت‌گذاری دلاری — چطور کار می‌کند

قیمت دلاری روی خود نسخه ذخیره می‌شود (`priceUsd`، به سِنت)، ولی چیزی که کل
سایت می‌خواند همان ستون تومانی `price` است. با هر تغییر نرخ، یک دستور SQL
قیمت تومانی همه‌ی نسخه‌های دلاری را بازنویسی می‌کند.

**چرا این‌طور:** `price` در ۲۰ جای سایت خوانده می‌شود. اگر قرار بود همه‌جا
تبدیل ارز انجام شود، هم کند می‌شد و هم ۲۰ فرصت برای باگ قیمتی. این‌طوری
هیچ‌کدام از آن ۲۰ جا تغییری لازم نداشت.

- نسخه‌های تومانی (`usdPriced=false`) با تغییر نرخ **دست نمی‌خورند** —
  محصولات فیزیکی و خدمات این‌طورند.
- **کمربند ایمنی:** اگر نرخ جدید بیش از ۵٪ با نرخ فعلی فرق داشته باشد،
  بار اول رد می‌شود و تایید دوباره می‌خواهد. یک صفر اضافه هنگام تایپ یعنی
  فروش کل فروشگاه به یک‌دهم قیمت.
- سرویس نرخ بیرونی فقط **پیشنهاد** می‌دهد؛ هیچ قیمتی خودکار عوض نمی‌شود.
  سرویس با `USD_RATE_URL` و `USD_RATE_UNIT` در `.env` قابل تعویض است.

---

## ۵. دام‌های این محیط — حتماً بخوان

این‌ها را با هزینه یاد گرفتیم. تکرارشان نکن.

### ۱) ابزار خط‌فرمان Prisma به Neon وصل نمی‌شود

`prisma db push` و `prisma migrate` خطای **P1001** می‌دهند (موتور Rust پشت
این شبکه به Neon نمی‌رسد). ولی درایور Node بدون مشکل وصل می‌شود.

**راه‌حل جایگزین ساخته شده:** برای هر تغییر schema، یک فایل SQL جدید در
`prisma/sql/` بساز (مثل `006_xxx.sql`) و بزن:

```
npm run db:migrate      # اجرای مهاجرت‌های اعمال‌نشده
npx prisma generate     # ⚠️ فراموش نکن
```

### ۲) بعد از `prisma generate` حتماً سرور را ری‌استارت کن

سرور در حال اجرا کلاینت قدیمی Prisma را نگه می‌دارد. اگر ری‌استارت نکنی،
خطای «Unknown field» یا «Cannot read properties of undefined» می‌گیری.
**این دو بار وقت تلف کرد.**

### ۳) مهلت تراکنش و کندی شبکه

Neon در فرانکفورت است و هر رفت‌وبرگشت ~۲۰۰-۵۰۰ میلی‌ثانیه طول می‌کشد.
مهلت پیش‌فرض ۵ ثانیه‌ای تراکنش Prisma **یک بار واقعاً تمام شد** و باعث شد
سفارش پرداخت‌شده ثبت شود ولی موجودی کم نشود و کد تخفیف مصرف‌شده علامت نخورد.

**قاعده:** تراکنش‌ها را کوتاه نگه دار، `timeout` صریح بده، و نوشتن حیاتی
(مثل ثبت وضعیت پرداخت) را جدا و اول انجام بده.
`npm run orders:repair` برای ترمیم چنین سفارش‌هایی ساخته شده است.

### ۴) `npm` نسخه ۱۱ اسکریپت‌های نصب را مسدود می‌کند

اگر پکیجی نصب کردی که postinstall دارد:
`npm approve-scripts <pkg>` بعد دوباره `npm install`.

### ۵) هرگز فایل‌های پروژه را با PowerShell ننویس

`Set-Content -Encoding utf8` یک BOM اول فایل می‌گذارد. یک بار `package.json`
را خراب کرد و **کل سایت ۵۰۰ داد**. از ابزار Write یا heredoc در bash استفاده کن.

### ۶) heredoc چندتایی در یک دستور bash شکننده است

نوشتن چند فایل بزرگ TSX با چند heredoc پشت سر هم خطای parse می‌دهد.
فایل‌ها را تک‌تک با Write بنویس.

### ۷) Satori (تصویرساز OG) فارسی را کامل پشتیبانی نمی‌کند

ترتیب کلمه‌ها را برعکس می‌چیند و فاصله‌ها را کشیده رسم می‌کند.
`src/lib/og-rtl.ts` این را جبران می‌کند. فونت هم باید دستی داده شود
(`src/assets/Vazirmatn-Bold.ttf`) وگرنه خطای
`lookupType: 5 - substFormat: 3` می‌دهد.

### ۸) تست‌ها را پشت سر هم اجرا نکن بدون پاک کردن OTP

فاصله‌ی اجباری ۶۰ ثانیه بین دو کد، ورود تست بعدی را بلاک می‌کند.
اسکریپت‌های تست خودشان `OtpCode` و `RateLimit` را پاک می‌کنند.

---

## ۶. اطلاعات دسترسی

| | |
|---|---|
| آدرس محلی | http://localhost:3000 |
| شماره ادمین | `09120000000` |
| کد تایید | در کادر بنفش روی خود صفحه‌ی ورود |
| کدهای تخفیف نمونه | `WELCOME20` (۲۰٪ تا سقف ۳۰۰ هزار) · `OFF500` (۵۰۰ هزار، سبد بالای ۳ میلیون) |

`.env` پر شده است (رشته‌ی اتصال Neon، AUTH_SECRET، حالت mock درگاه).

---

## ۷. دستورها

```
npm run dev              روشن کردن سایت
npm test                 اجرای هر ۱۱۳ تست
npm run db:migrate       اعمال مهاجرت‌های جدید
npm run db:seed          پر کردن دوباره‌ی محصولات نمونه (⚠️ محصولات فعلی پاک می‌شوند)
npm run limits:reset     صفر کردن شمارنده‌های محدودیت نرخ
npm run orders:repair    ترمیم سفارش‌های ناقص
npm run db:studio        مشاهده‌ی مستقیم دیتابیس
npm run products:check   بررسی فایل CSV محصولات، بدون تغییر دیتابیس
npm run products:import  وارد کردن گروهی محصولات از CSV
npm run catalog:check    بررسی فهرست محصولات فروشگاه، بدون تغییر
npm run catalog:apply    ساختن محصولات نبوده از prisma/real-catalog.mjs
npm run catalog:apply -- --overwrite   بازنویسی محصولات موجود از روی فایل
```

`catalog:apply` هیچ چیزی حذف نمی‌کند و به‌طور پیش‌فرض به محصول **موجود** هم
دست نمی‌زند — فقط چیزی را می‌سازد که نبوده.

⚠️ `--overwrite` عنوان، توضیحات، تصاویر، قیمت، موجودی و وضعیت فعال هر محصولی
را که slug‌اش در فایل کاتالوگ هست از روی همان فایل بازنویسی می‌کند، و محصول
خارج از فهرست (از جمله محصولی که مدیر خودش در پنل ساخته) را به `legacy-…`
تغییر نام می‌دهد و پنهان می‌کند. یعنی **هر ویرایشی که در پنل ادمین انجام شده
از بین می‌رود**. فقط وقتی بزنش که واقعاً می‌خواهی فایل کاتالوگ منبع حقیقت
باشد.

راهنمای فارسی فایل CSV برای صاحب پروژه: `data/products-guide.md`

**قبل از اعلام «تمام شد» همیشه `npm test` بزن.** تست‌ها با سرور و دیتابیس
واقعی کار می‌کنند، پس سرور باید بالا باشد.

---

## ۸. یک نکته‌ی نیمه‌کاره

تصویر اشتراک‌گذاری (`/opengraph-image`) کار می‌کند و متن فارسی درست است،
ولی **فاصله‌ی بین کلمه‌ها کمی زیاد** به نظر می‌رسد. محدودیت خود Satori است.
اولویت پایینی دارد؛ اگر کاربر اشاره نکرد، دست نزن.

---

## ۹. آخرین تغییر این جلسه

`src/proxy.ts` یک نگهبان سریع برای مسیرهای خصوصی است، ولی `/checkout`
**عمداً از فهرستش برداشته شد**. دلیلش در خود فایل کامنت شده: طبق طراحی،
مرحله‌ی اول تسویه‌حساب ورود درون‌صفحه‌ای است و مسدود کردن مسیر آن مرحله را
بی‌استفاده می‌کرد. اگر دوباره اضافه‌اش کردی، فرم ورود داخل `CheckoutFlow`
دیگر هرگز دیده نمی‌شود.

وضعیت تایید شده در پایان جلسه:

```
200  /            200  /products    200  /cart
200  /login       200  /checkout    307  /account (هدایت به ورود)
307  /admin (هدایت به ورود)
۱۱۳ از ۱۱۳ تست موفق · TypeScript بدون خطا
```

---

## ۱۰. سایت زنده روی رانفلر

راهنمای کامل قدم‌به‌قدم: **`DEPLOY-RUNFLARE.md`**

لیارا حذف شد. `liara.json`، `.liaraignore` و `DEPLOY-LIARA.md` از مخزن
برداشته شده‌اند.

```
runflare deploy         آپلود نسخه‌ی جدید (بار اول: انتخاب پروژه و سرویس)
runflare deploy -y      همان، با انتخاب قبلیِ کش‌شده
LIVE_URL=https://… npm run test:live      تست سرتاسری روی سایت زنده
```

### شکل استقرار

دو سرویس داخل یک پروژه، که منابع پروژه بینشان تقسیم می‌شود:

| سرویس | چیست |
|---|---|
| `giftland` | خود سایت (NodeJS) |
| Postgres | دیتابیس — جایگزین Neon شد |

**چرا Neon کنار گذاشته شد:** سرور رانفلر داخل ایران است و Neon روی AWS
فرانکفورت. هم احتمال بلاک شدن IP ایران وجود دارد، هم رفت‌وبرگشت شبکه روی
تک‌تک کوئری‌های هر صفحه می‌نشیند.

### اندازه‌ی منابع (اندازه‌گیری‌شده، نه حدس)

| | مقدار |
|---|---|
| پیک رم هنگام `npm run build` | ۹۵۳ MB |
| رم در حالت اجرا (گرم) | ۱۷۷ MB |
| زمان بیلد روی CPU دسکتاپ | ۵۸ ثانیه |
| `node_modules` | ۶۹۱ MB |
| `.next/standalone` بعد از بیلد | ۳۵ MB |

یعنی گلوگاه **بیلد** است، نه اجرا. اگر رم را برای اجرا اندازه بگیری، بیلد
بعدی OOM می‌شود.

### دام مهم: `next start` کار نمی‌کند

چون `output: "standalone"` روشن است، `next start` را خود Next رد می‌کند.
نقطه‌ی شروع درست `node .next/standalone/server.js` است — همان که `npm start`
اجرا می‌کند.

و چون خروجی standalone عمداً `public/` و `.next/static` را داخل خودش کپی
نمی‌کند، `npm run build` بعد از `next build` اسکریپت
`scripts/prepare-standalone.mjs` را هم می‌زند. **بدون آن قدم، سایت بالا
می‌آید ولی بی‌استایل و بی‌تصویر، و هیچ خطایی هم نمی‌دهد.**

### قاعده‌ای که از لیارا یاد گرفتیم

بعضی سرویس‌های ابری هنگام بیلد `next.config` خودشان را جایگزین می‌کنند و
`headers()` ما را دور می‌ریزند — روی لیارا دقیقاً همین شد و هدرهای امنیتی
روی سایت زنده اعمال نمی‌شدند در حالی که محلی درست بودند.

**قاعده:** هر تنظیمی که حتماً باید روی سرور اعمال شود را فقط در
`next.config` نگذار. هدرهای امنیتی الان در `src/proxy.ts` هستند.

### ⛔ پیش از افتتاح واقعی

- `DEMO_MODE` ست نباشد. تا وقتی روشن است، هر کسی می‌تواند با شماره‌ی هر کس
  دیگری وارد حساب او شود.
- `ZARINPAL_MODE` و `SMS_PROVIDER` هنوز `mock` و `console` هستند — یعنی
  پرداخت واقعی انجام نمی‌شود و کد تایید فقط در لاگ سرور چاپ می‌شود.

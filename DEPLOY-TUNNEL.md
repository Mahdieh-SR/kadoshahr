# لایو کردن سایت با کلادفلر تانل (بدون هزینه‌ی میزبانی)

جایگزین موقت `DEPLOY-RUNFLARE.md` برای دوره‌ای که نمی‌خواهیم هزینه‌ی رانفلر
بدهیم. سایت روی همین کامپیوتر اجرا می‌شود و کلادفلر آن را روی
`kadoshahr.ir` به اینترنت می‌آورد.

**دیتابیس همان Neon است و دست نمی‌خورد** — بررسی و تست شد، سالم است.

---

## این روش چه چیزی را حل می‌کند و چه چیزی را نه

| | |
|---|---|
| هزینه‌ی میزبانی | **صفر** — پلن رایگان کلادفلر |
| گواهی SSL | **خودکار و رایگان** — مشکل باز `TRAEFIK DEFAULT CERT` هم با همین حل می‌شود |
| تغییر کد | **هیچ** — نه در سایت، نه در دیتابیس |
| ⚠️ شرط | کامپیوتر باید **روشن و به اینترنت وصل** بماند. خاموش/خواب = سایت پایین |
| ⚠️ سرعت | به آپلود اینترنت خانگی تو وابسته است |

---

## بررسی‌های انجام‌شده (لازم نیست تکرار کنی)

- بیلد پروداکشن موفق است و سرور standalone بالا می‌آید ✅
- صفحه‌ی اصلی و صفحه‌ی محصول با داده‌ی واقعی Neon رندر می‌شوند (HTTP 200) ✅
- `sitemap.xml` آدرس `https://kadoshahr.ir` را درست می‌سازد ✅
- `cloudflared` نسخه‌ی 2026.9.0 در `C:\Users\NikRayaneh\cloudflared\` نصب شد ✅
- **مسیر شبکه به کلادفلر باز است** — این مهم‌ترین تست بود:

| میزبان | نتیجه |
|---|---|
| `region1.v2.argotunnel.com:7844` | باز ✅ |
| `region2.v2.argotunnel.com:7844` | باز ✅ |
| `api.cloudflare.com:443` | باز ✅ |
| `api.trycloudflare.com:443` | **فیلتر** ❌ |

آخری فقط برای تانل موقتِ بی‌حساب است و به آن نیازی نداریم. تانل نام‌دار —
همان چیزی که این سند می‌سازد — از دو میزبان اول استفاده می‌کند و باز است.

---

## قدم ۱ — دامنه را به کلادفلر ببر ⚠️ این کار توست

الان نیم‌سرورهای `kadoshahr.ir` روی **رانفلر** است:

```
ns1.runflare-dns.com
ns2.runflare-dns.com
```

تانل نام‌دار حتماً نیاز دارد DNS دامنه روی خود کلادفلر باشد. ضمناً اگر
رانفلر را کامل رها کنی، همین DNS هم ممکن است قطع شود و سایت حتی با تانل
هم بالا نیاید.

۱. در [dash.cloudflare.com](https://dash.cloudflare.com) حساب بساز
۲. **Add a site** → `kadoshahr.ir` → پلن **Free**
۳. کلادفلر دو نیم‌سرور می‌دهد، مثل `xxx.ns.cloudflare.com`
۴. در پنل ثبت دامنه ([ir.nic.ir](https://ir.nic.ir)) نیم‌سرورها را به همان‌ها عوض کن
۵. منتظر بمان تا کلادفلر دامنه را `Active` نشان دهد (برای `.ir` معمولاً چند ساعت)

> رکورد `A` فعلی به `195.214.235.219` (رانفلر) اشاره دارد. لازم نیست دستی
> پاکش کنی؛ قدم ۳ خودش رکورد درست را می‌نشاند.

### تنظیم SSL

بعد از `Active` شدن، در **SSL/TLS → Overview** حالت را روی **Full** بگذار.
روی `Flexible` نگذار — با تانل باعث حلقه‌ی ریدایرکت می‌شود.

---

## قدم ۲ — ورود و ساخت تانل

بعد از `Active` شدن دامنه، این‌ها را بزن. اولی مرورگر باز می‌کند تا
دسترسی را تایید کنی:

```
cd %USERPROFILE%\cloudflared
cloudflared.exe tunnel login
cloudflared.exe tunnel create kadoshahr
```

دستور دوم یک فایل `.json` در `C:\Users\NikRayaneh\.cloudflared\` می‌سازد و
یک **UUID** چاپ می‌کند. آن UUID را برای قدم بعد لازم داری.

---

## قدم ۳ — وصل کردن دامنه به تانل

```
cloudflared.exe tunnel route dns --overwrite-dns kadoshahr kadoshahr.ir
cloudflared.exe tunnel route dns --overwrite-dns kadoshahr www.kadoshahr.ir
```

این‌ها خودشان رکورد `CNAME` را در کلادفلر می‌سازند.

> ⚠️ `--overwrite-dns` لازم است. موقع افزودن دامنه، کلادفلر رکوردهای `A`
> قبلی را که به IP رانفلر (`195.214.235.219`) اشاره می‌کنند از DNS فعلی
> کپی می‌کند. بدون این سوییچ دستور با خطای «record already exists» رد
> می‌شود. با آن، رکورد `A` مرده با `CNAME` تانل جایگزین می‌شود.
>
> رکورد `TXT` مربوط به رانفلر بماند — بی‌ضرر است و اگر خواستی برگردی به
> کار می‌آید.

---

## قدم ۴ — فایل تنظیمات

فایل `C:\Users\NikRayaneh\.cloudflared\config.yml` را با این محتوا بساز
(`<UUID>` را از قدم ۲ بگذار):

```yaml
tunnel: kadoshahr
credentials-file: C:\Users\NikRayaneh\.cloudflared\6dcfd498-d679-4870-88dd-9a896c4c0a10.json

# چرا http2 و نه پیش‌فرض quic؟
# quic روی UDP پورت ۷۸۴۴ کار می‌کند که روی اینترنت ایران معمولاً ناپایدار
# است. TCP همان پورت تست و باز بود، پس مستقیم سراغ http2 می‌رویم.
protocol: http2

ingress:
  - hostname: kadoshahr.ir
    service: http://localhost:3000
  - hostname: www.kadoshahr.ir
    service: http://localhost:3000
  - service: http_status:404
```

> ⚠️ **`httpHostHeader` را اضافه نکن.** سایت از Server Actions استفاده
> می‌کند و Next هدر `Origin` را با `Host` مقایسه می‌کند؛ اگر `Host` را
> بازنویسی کنی همه‌ی فرم‌ها (ورود، سبد خرید، پرداخت) با خطای
> «Invalid Server Actions request» رد می‌شوند. حالت پیش‌فرض درست است.

---

## قدم ۵ — اجرا

دو چیز باید هم‌زمان بالا باشند.

**الف) سایت** — در پوشه‌ی پروژه:

```
npm run build
scripts\serve.cmd
```

`serve.cmd` متغیر `APP_BASE_URL=https://kadoshahr.ir` را ست می‌کند. این
متغیر **زمان اجرا** خوانده می‌شود، پس اگر بعداً دامنه عوض شد فقط همین فایل
و یک ری‌استارت کافی است — بیلد مجدد لازم نیست.

**ب) تانل** — در یک پنجره‌ی دیگر:

```
%USERPROFILE%\cloudflared\cloudflared.exe tunnel run kadoshahr
```

---

## ⚠️ ساعت سیستم — این را جدی بگیر

روی همین کامپیوتر سرویس زمان ویندوز **Disabled** بود و ساعت ۲ ساعت دریفت
کرده بود، ضمناً منطقه‌ی زمانی روی `Syria Standard Time` (UTC+۳) است نه
ایران (UTC+۳:۳۰).

چرا برای این سایت مهم است؟ سرور روی همین دستگاه اجرا می‌شود، پس ساعت
اشتباه یعنی:

- `createdAt` سفارش‌ها با خطا در Neon ثبت می‌شود
- انقضای کد تایید پیامکی و `RateLimit` غلط حساب می‌شود
- کوکی نشست (next-auth) زودتر یا دیرتر منقضی می‌شود
- گواهی‌های تازه‌صادرشده با `CERT_NOT_YET_VALID` رد می‌شوند — یک بار همین
  باعث شد `npm run test:live` بشکند در حالی که سایت سالم بود
- وقتی زرین‌پال از `mock` به واقعی برود، اختلاف زمان می‌تواند تاییدیه‌ی
  پرداخت را رد کند

**درست کردن دائمی** — PowerShell را «Run as administrator» باز کن:

```powershell
Set-TimeZone -Id "Iran Standard Time"
Set-Service W32Time -StartupType Automatic
Start-Service W32Time
w32tm /resync /force
```

اگر `resync` خطا داد (دسترسی به سرور زمان مایکروسافت گاهی بسته است):

```powershell
w32tm /config /manualpeerlist:"pool.ntp.org,0x9 time.google.com,0x9" /syncfromflags:manual /update
Restart-Service W32Time
w32tm /resync /force
```

> جلو بردن دستی ساعت کافی **نیست**. تا وقتی منطقه‌ی زمانی غلط باشد، ساعتی
> که روی صفحه درست به نظر می‌رسد UTC غلط تولید می‌کند — و برنامه‌ها با UTC
> کار می‌کنند. تا وقتی هم `W32Time` روی Automatic نباشد، ساعت دوباره دریفت
> می‌کند و چند هفته بعد همین مشکل برمی‌گردد.

**بررسی:** این دو باید حداکثر چند ثانیه اختلاف داشته باشند.

```
node -e "console.log('سیستم:', new Date().toISOString())"
curl -sI https://www.google.com
```

---

## قدم ۶ — دائمی کردن

همه‌ی این کارها در یک اسکریپت جمع شده. **PowerShell را با Run as
administrator** باز کن و بزن:

```powershell
powershell -ExecutionPolicy Bypass -File "scripts\setup-host.ps1"
```

چهار کار می‌کند:

| | کار |
|---|---|
| ۱ | منطقه‌ی زمانی را روی ایران و `W32Time` را روی Automatic می‌گذارد و ساعت را همگام می‌کند |
| ۲ | `cloudflared` را به‌عنوان سرویس ویندوز نصب می‌کند |
| ۳ | تسک `Kadoshahr Site` را می‌سازد: اجرا با روشن شدن ویندوز، زیر حساب SYSTEM، و اگر پروسه مرد یک دقیقه بعد دوباره بلند می‌شود |
| ۴ | خواب و هایبرنیت را خاموش می‌کند |

چرا زیر حساب **SYSTEM**؟ چون تسک باید بدون ورود کاربر هم اجرا شود. اگر برق
برود و کامپیوتر خودش بالا بیاید، سایت نباید منتظر بماند تا کسی لاگین کند.

اسکریپت بی‌خطر است اگر دوباره اجرا شود: سرویس موجود را دست نمی‌زند و تسک
قبلی را حذف و از نو می‌سازد.

### بررسی بعد از اجرا

```powershell
Get-Service cloudflared
Get-ScheduledTask -TaskName "Kadoshahr Site"
```

و لاگ خود سایت در `logs\site.log` است — چون Task Scheduler هیچ پنجره‌ای
ندارد که خطا را نشان دهد، `serve.cmd` خروجی را آنجا می‌نویسد.

---

## بعد از بالا آمدن، این‌ها را تست کن

```
npm run test:live
```

و دستی: ورود با شماره، افزودن به سبد، و یک پرداخت آزمایشی تا آخر (درگاه
هنوز روی `mock` است).

---

## دام‌هایی که سر راه خوردیم

اگر دوباره این کار را از صفر کردی، این پنج مورد بیشترین وقت را گرفتند.

### ۱. `cloudflared service install` سرویس را بدون آرگومان ثبت می‌کند

بعد از نصب، `binPath` سرویس فقط این بود:

```
C:\Users\NikRayaneh\cloudflared\cloudflared.exe
```

نه `tunnel run`، نه `--config`. نتیجه: سرویس «Running» نشان می‌داد ولی
کلادفلر می‌گفت «تانل هیچ اتصال فعالی ندارد» و سایت خطای ۵۳۰ می‌داد.

`setup-host.ps1` این را با `sc.exe config` اصلاح می‌کند. اگر دستی نصب
کردی، حتماً `binPath` را چک کن:

```powershell
(Get-CimInstance Win32_Service -Filter "Name='Cloudflared'").PathName
```

### ۲. `Restart-Service Cloudflared` بی‌نهایت منتظر می‌ماند

پروسه‌ی قدیمیِ سرویس (همان که بدون آرگومان بالا آمده) به سیگنال توقف جواب
نمی‌دهد و سرویس در `StopPending` گیر می‌کند. باید با مهلت صبر کرد و بعد
پروسه را `Stop-Process -Force` کرد.

### ۳. فایل `.ps1` فارسی حتماً باید UTF-8 **با BOM** باشد

ویندوز پاورشل ۵.۱ فایل بدون BOM را ANSI می‌خواند؛ متن فارسی به
`§Û„Ø§Ù†` تبدیل می‌شود و پارسر با `Unexpected token` می‌شکند. اگر اسکریپت
را ویرایش کردی، دوباره با BOM ذخیره‌اش کن:

```powershell
$p = "scripts\setup-host.ps1"
$c = Get-Content -Raw -Encoding UTF8 $p
[IO.File]::WriteAllText($p, $c, (New-Object Text.UTF8Encoding($true)))
```

### ۴. `node` از nvm نصب است و مسیرش به پروفایل کاربر می‌رود

```
C:\nvm4w\nodejs  →  C:\Users\NikRayaneh\AppData\Local\nvm\v24.19.0
```

تسک زیر حساب SYSTEM اجرا می‌شود. `serve.cmd` حالا `node` را صریح پیدا
می‌کند و اگر نیافت در `logs\site.log` می‌نویسد. ⚠️ اگر با `nvm use` نسخه‌ی
node را عوض کنی، بعدش حتماً سایت را تست کن.

### ۵. یکی از دو IP کلادفلر از اینترنت ایران بسته است

ریشه‌ی دامنه روی دو IP انی‌کست می‌نشیند (مثلاً `188.114.96.3` و
`188.114.97.3`). روی این اتصال، یکی باز است و دیگری تایم‌اوت می‌خورد —
همان رنجی که دیسکورد را هم می‌بندد.

- مرورگرها خودشان روی IP سالم می‌افتند، پس بازدیدکننده معمولاً مشکلی
  نمی‌بیند (گاهی با کمی تأخیر).
- ولی `curl` و `npm run test:live` تصادفی انتخاب می‌کنند و ممکن است
  بی‌دلیل شکست بخورند. برای تست از این کامپیوتر، IP سالم را مجبور کن:

```
curl --resolve kadoshahr.ir:443:188.114.96.3 https://kadoshahr.ir/
```

این ایراد پیکربندی **نیست** — انتخاب IP در پلن رایگان دست ما نیست. برای
اطمینان از سلامت واقعی سایت، از یک شبکه‌ی دیگر تست کن.

---

## برگشت به رانفلر

هر وقت خواستی برگردی: `DEPLOY-RUNFLARE.md` را دنبال کن. فقط یادت باشد
نیم‌سرورها حالا روی کلادفلر است، پس به‌جای عوض کردن نیم‌سرور، در کلادفلر
رکورد `A` را به IP رانفلر برگردان و تانل را متوقف کن.
